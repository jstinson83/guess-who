{
  "name": "guess-who-frontend-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright tests for the static frontend in backend/src/main/resources/static.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
